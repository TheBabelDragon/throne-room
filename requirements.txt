rich>=13.0.0
redis>=5.0.0
numpy>=1.24.0
matplotlib>=3.7.0
# optional — field-head HUD checkpoints AND language-arm decoder
#   pip install torch
# CPU-only fallback:
#   pip install typing-extensions jinja2 filelock sympy networkx
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# from the repo root: pip install -e ".[head-torch]"
